pipeline {
	agent { kubernetes {
         yaml '''
            apiVersion: v1
            kind: Deployment
            spec:
              replicas: 2
            spec:
             containers:
             - name: debian
            image: debian:latest
            '''
       }
    }	
	stages {
//-------------------------------------------------- begin stages 

stage  ('Кубернетес') {
//
//
  	 steps {
	  sh 'uname -a'	  
	  } 
//--------------------------------------------------	
      }
//--------------------------------------------------End stage
    }
//--------------------------------------------------End pipline
}